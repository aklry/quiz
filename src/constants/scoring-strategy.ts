// 应用评分策略枚举
const APP_SCORING_STRATEGY_ENUM = {
  // 自定义
  CUSTOM: 0,
  // AI
  AI: 1
}

// 应用评分策略映射
const APP_SCORING_STRATEGY_MAP = ['自定义', 'AI']

export default {
  APP_SCORING_STRATEGY_ENUM,
  APP_SCORING_STRATEGY_MAP
}
